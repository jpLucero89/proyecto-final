spring:
  mail: 
    host: smtp.gmail.com
    port: 587
    protocol: smtp
    username: vinotecaonline20@gmail.com
    password: hnxpmtacgxkrwicm
    properties:
        mail:
            smtp:              
                auth: true
                starttls:
                    required: true
                    enable: true
                quitwait: false
    
    
  datasource: 
    url: jdbc:mysql://localhost:3306/proyecto_final?allowPublicKeyRetrieval=true&useSSL=false&useTimezone=true&serverTimezone=GMT&characterEncoding=UTF-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver 
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        open-in-view: false
  thymeleaf:
    cache: false 
server:
  error:
    whitelabel:
      enabled: false
  servlet:
    session:
      cookie:
        http-only: true
      tracking-modes:
      - cookie
      
      